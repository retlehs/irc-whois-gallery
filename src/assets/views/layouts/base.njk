<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    {% set pageTitle = title if title != site.siteName else site.siteName %}
    {% set fullTitle = pageTitle + (" | " + site.siteName if pageTitle != site.siteName else "") %}
    <title>{{ fullTitle }}</title>
    <link rel="stylesheet" href="{{ '/assets/styles.css' | url }}"/>
    <link rel="icon" type="image/svg+xml" href="{{ '/assets/images/favicon.svg' | url }}">
    {% metagen
      title = fullTitle,
      desc = description or site.description,
      url = site.url + page.url,
      img = site.url + "/assets/images/whois-kano-vortex.png",
      img_alt = "IRC whois theme screenshot",
      type = "website",
      twitter_card_type = "summary_large_image"
    %}

    {% if ( index == false) %}
      <meta name="robots" content="noindex,nofollow">
    {% endif %}

    <script>
      (function(c,l,a,r,i,t,y){
        c[a]=c[a]||function(){(c[a].q=c[a].q||[]).push(arguments)};
        t=l.createElement(r);t.async=1;t.src="https://www.clarity.ms/tag/"+i;
        y=l.getElementsByTagName(r)[0];y.parentNode.insertBefore(t,y);
      })(window, document, "clarity", "script", "sxtf6rm81h");
    </script>
  </head>
  <body>
    <a class="visually-hidden" href="#main">Skip to content</a>
    
    <header>
      {% include "partials/navigation.njk" %}
    </header>

    <main id="main" tabindex="-1">
      <header id="page-header">
        <h1>{{ title }}</h1>
      </header>
      {{ content | safe }}
    </main>

    <footer id="footer">
      <p>Preserving digital history, one /whois at a time.</p>
      <p><a href="https://github.com/retlehs/irc-whois-gallery">GitHub</a></p>
      <p><a href="https://retlehs.github.io/nfo-viewer/">NFO Viewer &amp; MP3 Scene NFO Gallery</a></p>
    </footer>

    <script src="{{ '/assets/main.js' | url }}"></script>
  </body>
</html>